[project]
name = "quotavera"
version = "0.1.0"
description = "Motore di pronostici sportivi: calcio, basket, tennis"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
